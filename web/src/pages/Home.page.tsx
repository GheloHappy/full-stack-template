export default function HomePage() {
  return (
    <main className="grid min-h-screen place-items-center px-6">
      <section className="max-w-xl text-center">
        <p className="text-sm font-semibold uppercase tracking-[0.2em] text-sky-700">
          React + Vite + Tailwind
        </p>
        <h1 className="mt-3 text-4xl font-bold tracking-tight text-slate-950 sm:text-6xl">
          Full Stack Template
        </h1>
        <p className="mt-5 text-lg leading-8 text-slate-600">
          Replace this route with your first feature. The API client, router,
          query cache, state store, and production container are ready.
        </p>
      </section>
    </main>
  )
}
